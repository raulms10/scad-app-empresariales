# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)

# Rutas del aplicativo ScadAppRentings
GET     /v1/agency/info              controllers.HomeController.getAgencyInfoService
GET     /v1/homes/all                controllers.HomeController.getAll
POST    /v1/homes/search             controllers.HomeController.search
POST    /v1/homes/myBooking/:token    controllers.HomeController.getBookingService(token :String)